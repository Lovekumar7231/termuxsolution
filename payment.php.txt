<?php
session_start();
// Agar user login nahi hai, to Login page par bhejo
if (!isset($_SESSION['user_id'])) {
    header("Location: login.html");
    exit();
}

// Product ki details URL se le rahe hain (Example: payment.php?name=WifiHacking&price=99)
$product_name = isset($_GET['name']) ? $_GET['name'] : "Premium Content";
$product_price = isset($_GET['price']) ? $_GET['price'] : "0";
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secure Payment Gateway</title>
    <link rel="stylesheet" href="style.css">
    <style>
        /* Payment Page Styles */
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: #050505;
        }
        .payment-card {
            background: #1a1a1a;
            padding: 30px;
            border: 1px solid var(--primary);
            text-align: center;
            width: 350px;
            border-radius: 10px;
        }
        .qr-image {
            width: 200px;
            height: 200px;
            margin: 20px 0;
            border: 2px solid white;
            background: white; /* Placeholder agar image na ho */
        }
        .price-tag {
            font-size: 24px;
            color: gold;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .info-text { color: #ccc; font-size: 14px; margin-bottom: 10px; }
    </style>
</head>
<body>

    <div class="payment-card">
        <h2 style="color: var(--primary);">Confirm Purchase</h2>
        <p style="color: white;">Item: <?php echo htmlspecialchars($product_name); ?></p>
        
        <div class="price-tag">₹<?php echo htmlspecialchars($product_price); ?></div>

        <div class="info-text">Scan QR Code to Pay via UPI/PhonePe</div>
        
        <img src="qr.png" alt="Payment QR Code" class="qr-image">

        <form action="process_payment.php" method="POST">
            <input type="hidden" name="product_name" value="<?php echo $product_name; ?>">
            <input type="hidden" name="amount" value="<?php echo $product_price; ?>">

            <div style="text-align: left; margin-top: 10px;">
                <label style="color: #888; font-size: 12px;">ENTER TRANSACTION ID (UTR):</label>
                <input type="text" name="transaction_id" class="input-field" placeholder="Example: 1234567890" required style="width: 100%; box-sizing: border-box;">
            </div>

            <button type="submit" class="btn-main" style="width: 100%; margin-top: 15px;">Verify & Unlock</button>
        </form>

        <br>
        <a href="index.php" style="color: red; text-decoration: none; font-size: 12px;">Cancel Transaction</a>
    </div>

</body>
</html>
