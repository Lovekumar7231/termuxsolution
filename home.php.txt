<?php
session_start();

// Check karo ki user login hai ya nahi. Agar nahi, to wapas login page bhej do.
if (!isset($_SESSION['username'])) {
    header("Location: login.php");
    exit();
}
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Home - Access Granted</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Courier New', monospace; }
        
        body {
            background-color: #050505;
            color: #00ff00;
            padding: 20px;
            background-image: radial-gradient(#1a1a1a 15%, transparent 16%);
            background-size: 30px 30px;
        }

        /* --- HEADER --- */
        .header {
            border-bottom: 2px solid #00ff00;
            padding-bottom: 15px;
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .user-info { font-size: 14px; }
        .user-name { font-weight: bold; text-transform: uppercase; color: #fff; }

        .logout-btn {
            background: #ff0000;
            color: #000;
            text-decoration: none;
            padding: 5px 15px;
            font-weight: bold;
            font-size: 12px;
            border: 1px solid #ff0000;
        }
        .logout-btn:hover { background: #000; color: #ff0000; }

        /* --- MAIN CONTENT GRID --- */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
        }

        .card {
            border: 1px solid #333;
            background: rgba(0, 0, 0, 0.8);
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: 0.3s;
        }

        .card:hover {
            border-color: #00ff00;
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.3);
        }

        .icon { font-size: 30px; margin-bottom: 10px; display: block; }
        .card h3 { font-size: 14px; text-transform: uppercase; letter-spacing: 1px; }

        /* --- STATUS TEXT --- */
        .status {
            margin-top: 50px;
            font-size: 12px;
            color: #666;
            text-align: center;
            animation: blink 2s infinite;
        }

        @keyframes blink { 50% { opacity: 0; } }
    </style>
</head>
<body>

    <div class="header">
        <div class="user-info">
            CONNECTED AGENT: <span class="user-name"><?php echo $_SESSION['username']; ?></span>
            <br>
            STATUS: <span style="color: #00ff00;">ONLINE</span>
        </div>
        <a href="logout.php" class="logout-btn">TERMINATE SESSION</a>
    </div>

    <h2 style="text-align: center; margin-bottom: 20px; text-shadow: -2px 0 red;">MAINFRAME CONTROL</h2>

    <div class="grid-container">
        <div class="card">
            <span class="icon">📂</span>
            <h3>Data Files</h3>
        </div>
        <div class="card">
            <span class="icon">📡</span>
            <h3>Network Status</h3>
        </div>
        <div class="card">
            <span class="icon">⚙️</span>
            <h3>Settings</h3>
        </div>
        <div class="card">
            <span class="icon">💬</span>
            <h3>Secure Chat</h3>
        </div>
    </div>

    <div class="status">
        SYSTEM SECURE... WAITING FOR COMMAND...
    </div>

</body>
</html>
